{
  "name": "guodu-prompts-engine",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/langchain",
    "packages/ai-sdk"
  ],
  "scripts": {
    "build": "tsc -b",
    "build:core": "bun run --filter guodu-prompt-engine-core build",
    "build:adapters": "bun run --filter guodu-prompt-engine-langchain build && bun run --filter guodu-prompt-engine-ai-sdk build",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "test": "bun test packages/*/src",
    "typecheck": "bun run --filter guodu-prompt-engine-core typecheck && bun run --filter guodu-prompt-engine-langchain typecheck && bun run --filter guodu-prompt-engine-ai-sdk typecheck",
    "release": "bun run scripts/release.ts",
    "release:dry": "bun run scripts/release.ts --dry"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "bumpp": "^11.1.0",
    "typescript": "^5.0.0",
    "vitepress": "^1.6.4"
  }
}
